# Branding

`branding/` stores source-controlled Lingxi branding inputs. The bootstrap script applies the colors, fallback language and custom content through Logto's supported Sign-in Experience Management API. Logo URLs should point to assets served by the deployment's HTTPS edge; no Logto source files are copied or patched.
