# JWT customizer

The JWT customizer is installed by `bootstrap/cli.py` through Logto's supported Management API. No Logto JavaScript source is mounted into the container. Keep any reviewed customizer source or change notes here if the upstream API requires a future update.
