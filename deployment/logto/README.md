# Logto deployment boundary

This directory contains deployment-facing notes only. LingxiIdentity does not copy, fork, or patch Logto source code. The Logto image is pinned in `deployment/compose.yaml` and is configured exclusively through supported environment variables and Management API calls.
