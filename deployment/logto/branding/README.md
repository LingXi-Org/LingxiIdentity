# Logto branding boundary

Branding inputs live in the repository-level `branding/` directory and are applied through the supported Sign-in Experience Management API during bootstrap. This directory intentionally contains no Logto source override.
